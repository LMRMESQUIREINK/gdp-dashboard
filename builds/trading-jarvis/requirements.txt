# ============================================================
# Trading Jarvis — Requirements (Pro build)
# ============================================================
# Exact pins, not floors/ceilings — same lesson learned building the
# Kryptera Strategy Lab Pro package: an open-ended pin can silently
# resolve to an incompatible release between two installs. This stack
# has no vectorbt/numba-style old-pandas constraint, so these are
# simply "whatever was verified working," pinned for reproducibility.
# ============================================================

anthropic==1.7.0
requests==2.34.2
pandas==3.0.6
numpy==2.4.6
